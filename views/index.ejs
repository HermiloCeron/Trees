<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
        <link rel="stylesheet" href="/css/app.css">
    </head>
    <body>
        <h1>Trees</h1>
        <div class="Trees">
        <div class="List">
        <ul>
            <% for(let i=0; i<trees.length; i++) { %>
                <li>
                    <a href="/trees/<%=userId%>/<%=trees[i].id%>"> <%=trees[i].name%> </a>
                    <form action="/trees/<%=userId%>/<%=trees[i].id; %>?_method=DELETE" method="POST">
                        <input type="submit" value="DELETE"/>
                    </form>
                    <a href="/trees/<%=userId%>/<%=trees[i].id; %>/edit">Edit</a>
                </li>
            <% } %>
        </ul>
        <nav class="New">
            <a href="/trees/<%=userId%>/new">Create a New Tree</a>
        </nav>
        </div>
    </div>
    </body>
</html>